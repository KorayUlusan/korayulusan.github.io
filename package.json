{
  "name": "korayulusan-site",
  "private": true,
  "devDependencies": {
    "tailwindcss": "^4.0.0"
  },
  "scripts": {
    "build:css": "tailwindcss -i src/styles/tailwind.css -o build/css/tailwind.css --minify",
    "watch:css": "tailwindcss -i src/styles/tailwind.css -o build/css/tailwind.css --watch"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.2.2"
  }
}